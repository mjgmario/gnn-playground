task: link_prediction
dataset: email_eu_core
models: [gcn, graphsage]
decoders: [dot, mlp]
seed: 42
epochs: 100
lr: 0.01
hidden_dim: 64
num_layers: 2
dropout: 0.5
weight_decay: 0.0
patience: 20
output_dir: outputs/link_prediction_email
